const APIConfig = {
    API : {
        endpoints: [
            {
                name: "adminQueries",
                endpoint: "https://6vpwcecvcd.execute-api.ap-south-1.amazonaws.com/beta",
            }
        ]
    }
}

export default APIConfig;